# Waffen Tactics — Current Status

Date: 2026-09-10

Linear project: `Waffen Tactics — Content, Runtime & Production`

Linear is the source of truth for issue status, ownership, dependencies, milestones, and user decisions. This file is a repository handoff snapshot; it does not replace the Linear workflow.

## Verified automated evidence

- Shared combat/data core: from `waffen-tactics/`, `python -W error -m pytest -q 'waffen-tactics\\tests'` — **397 passed, 25 skipped, 22 subtests passed**, with no warnings.
- Backend: from `waffen-tactics-web/backend/`, `python -W error -m pytest -q` — **317 passed, 8 skipped, 5 subtests passed**, with no warnings.
- Frontend: `npm run typecheck` — pass; `npm run lint` — pass; `npm exec -- vitest run` — **129 passed across 8 files**.
- Frontend production build: `npm run build` — pass. Vite reports only stale Browserslist data; the build succeeds.
- `python -m compileall -q waffen-tactics\\src waffen-tactics\\tests` — pass.
- Dataset validators: `PYTHONIOENCODING=utf-8 python tools/scripts/validate_units.py` passes from the repository root and an unrelated working directory, checking **52 units and 21 traits**; malformed non-object unit/trait records now produce structured validation errors (`DEF-232`); `python waffen-tactics/scripts/validate_all_skills.py` checks all **52 unit skills** successfully and rejects missing, null, or malformed canonical skills (`DEF-229`, `DEF-231`).

The recent runtime hardening includes canonical combat/replay event paths, fail-closed combat execution, progression parity, shared stat-formula/position validation, fail-closed frontend formation initialization and position desync reporting (`DEF-172`), fail-closed canonical HP mutation (`DEF-222`), strict canonical effect/DoT replay boundaries (`DEF-225`), fail-closed canonical unit skill loading (`DEF-230`), loopback-only production API and Vite bindings behind Caddy (`DEF-226`, `DEF-227`), public health-response path redaction (`DEF-228`), canonical HP/max-HP effect expiration, canonical shield application/expiration, shield-aware damage resolution, and shield-preserving DoT replay (`DEF-211`, `DEF-213`, `DEF-214`, `DEF-215`, `DEF-216`). Strict async/replay test teardown is also clean (`DEF-223`). Dataset validator path/schema alignment, canonical skill-presence validation, and malformed-record fail-closed handling are complete (`DEF-229`, `DEF-231`, `DEF-232`). DEF-233 now documents and test-enforces the production combat owner while quarantining the stale prototype combat path; it does not change the active damage formula. DEF-234 now rejects non-object or non-JSON combat request bodies at the route boundary using the existing missing-token contract. DEF-238 now rejects malformed or non-positive admin pagination parameters before database access with a shared validation helper. DEF-239 now gives authenticated game-lifecycle failures a stable redacted 500 response with correlation ID instead of exposing raw exception text. DEF-240 now gives unexpected player-action failures the same stable redacted 500 contract through the shared action boundary. DEF-241 now gives unexpected admin failures the same stable redacted 500 contract with request correlation without changing auth or successful payloads. DEF-242 now fails closed when required player-state enrichment cannot complete, builds bench projections without partial object mutation, and preserves the safe lifecycle/action/combat error boundaries. DEF-243 now fails closed on malformed embedded replay checkpoints instead of swallowing structural validation errors and continuing reconstruction. DEF-245 now propagates canonical event-delivery failures through every canonical emitter and the shared dispatcher/backend boundary instead of allowing a result with missing events. DEF-246 now validates the complete runtime effect collection before reverting state, and fails before emitting `effect_expired` when removal cannot be prepared or verified. The DEF-222 extension now also fails closed for canonical death HP and simulator HP-mirror mutation failures. DEF-247 now propagates death-processing failures through both direct and scheduled authoritative paths instead of returning a winner without `unit_died`. DEF-248 now rejects missing or unknown unit identities in backend death, heal, HP-regen, and DoT-application replay events and rejects unsupported replay event types instead of silently consuming malformed events. DEF-249 now rejects missing or unknown unit identities in the frontend reducer before the affected mutation, records a typed replay desync, and stops the replay timer without advancing the playhead. These changes have no associated legacy-archive or permanent-content update.

DEF-250 now extends strict frontend replay identity validation to attack, mana, buff, shield, and effect mutation handlers. These changes have no associated legacy-archive or permanent-content update.

DEF-251 now rejects unsupported frontend replay event types with typed sequence-aware validation errors while preserving explicit metadata/no-op compatibility branches. These changes have no associated legacy-archive or permanent-content update.

DEF-252 now fails closed when canonical damage cannot apply and verify target HP, shield, or an explicitly supplied HP mirror, rolling back partial state before event delivery. These changes have no associated legacy-archive or permanent-content update.

DEF-253 now fails closed when canonical mana cannot apply and verify recipient mana or an explicitly supplied mana mirror, rolling back partial state before event delivery. These changes have no associated legacy-archive or permanent-content update.

DEF-254 now fails closed when canonical stun cannot apply and verify target stun fields/effect state, rejecting dead targets before mutation. These changes have no associated legacy-archive or permanent-content update.

DEF-255 now gives HP-regen rewards one canonical mutation per intended recipient, avoiding callback-dependent double application and source-only team events. These changes have no associated legacy-archive or permanent-content update.

DEF-256 now fails closed when canonical HP-regen mutation is rejected or ignored, and skips dead targets before event delivery. These changes have no associated legacy-archive or permanent-content update.

DEF-257 now makes canonical shield application atomic: rejected or silently ignored effect writes roll back the shield/effect state before raising, and no `shield_applied` event is delivered after a partial mutation. These changes have no associated legacy-archive or permanent-content update.

DEF-258 now makes persistent canonical stat buffs atomic across the immediate stat mutation and effect installation, rolling back rejected or silently ignored effect writes before raising and before delivering `stat_buff`. These changes have no associated legacy-archive or permanent-content update.

DEF-259 now makes canonical passive-effect replacement atomic by committing one verified final effect collection before replacement/application events, rolling back rejected or silently ignored writes, and preserving the successful expiration-before-application event order. These changes have no associated legacy-archive or permanent-content update.

DEF-260 now fails closed when expired DoT removal cannot be completed or verified, preventing a missing `damage_over_time_expired` event after a partial tick path while preserving valid removal and event ordering. These changes have no associated legacy-archive or permanent-content update.

DEF-261 now commits per-second HP regeneration through the canonical unit mutation before advancing the simulator HP mirror, rolls back canonical HP if downstream event delivery fails, and preserves direct no-callback compatibility. Symmetric regression coverage proves rejected setters leave unit HP, mirrors, logs, and success events unchanged. These changes have no associated legacy-archive or permanent-content update.

DEF-262 now routes Team A and Team B through one shared per-second processing helper and isolates the canonical-first HP/mirror boundary, preserving existing stat formulas, mana routing, side-specific payloads, amplifier behavior, and no-callback compatibility. These changes have no associated legacy-archive or permanent-content update.

DEF-263 now routes start-of-combat per-round HP buffs for both teams through one canonical-first helper, commits the simulator mirror and success log only after `emit_heal` succeeds, rolls back canonical HP after downstream callback failure, and sends the event through the simulator sink for replay metadata. The separate `skip_per_round_buffs` contract remains unchanged and tracked by DEF-235. These changes have no associated legacy-archive or permanent-content update.

DEF-265 now routes live object-unit modular stat-buff rewards through `emit_stat_buff` exactly once, supports the canonical singular/plural stat fields, preserves percentage semantics, and records successful permanent deltas for post-combat persistence. Regression coverage proves single and multi-stat rewards no longer double-apply and keeps active simulator values exact. These changes have no associated legacy-archive or permanent-content update.

DEF-266 now routes canonical tiered `on_enemy_death` and `on_ally_death` records from `modular_effects` into the active combat unit effects, keeps those rewards out of static start-of-combat stats, and limits each death trigger to surviving units on the reacting side. Canonical Streamer, Denciak, XN Waffen/KGB, and self-targeted effect contracts are covered by deterministic integration tests. These changes have no associated legacy-archive or permanent-content update. Canonical periodic/threshold routing remains explicitly tracked by `DEF-267`.

DEF-267 now routes canonical `per_second`, `per_round`, and `on_ally_hp_below` records through the shared simulator lifecycle. Periodic stat rewards use non-persistent canonical emissions, round HP rewards preserve round scaling and HP mirrors, and threshold healing requires a real threshold crossing with trigger state. Deterministic tests cover canonical Srebrna Gwardia, Starokurwy, and Wygnaniec scenarios; no canonical values or legacy content changed.

## Audits and artifacts

- `docs/BALANCE_AUDIT_2026-09-09.md` and `.json` contain the current seeded balance/economy audit. It reports system-level findings but does not authorize autonomous unit or trait tuning.
- `docs/OPPONENT_VARIETY_AUDIT_2026-09-09.md` and `.json` contain the current seeded opponent-behavior classification.
- `docs/SEEDED_SCENARIO_MATRIX_2026-09-09.md` and `.json` contain the deterministic scenario evidence.
- `docs/RELEASE_VALIDATION_GATE.md` is the release checklist and distinguishes automated evidence from runtime/manual acceptance.

## Open gates

- `DEF-164` is `In Review` with `Needs Manual Test` and `Blocked`. Its completed runtime/release blockers include `DEF-200`, `DEF-209`, `DEF-210`, `DEF-211`, `DEF-213`, `DEF-214`, `DEF-215`, `DEF-216`, `DEF-219`, `DEF-220`, `DEF-222`, `DEF-225`, `DEF-242`, `DEF-243`, `DEF-244`, `DEF-245`, `DEF-246`, `DEF-247`, `DEF-248`, `DEF-249`, `DEF-250`, `DEF-251`, `DEF-252`, `DEF-253`, `DEF-254`, `DEF-255`, `DEF-256`, `DEF-257`, `DEF-258`, `DEF-259`, `DEF-260`, `DEF-261`, `DEF-263`, `DEF-265`, `DEF-266`, `DEF-267`, `DEF-273`, `DEF-274`, `DEF-275`, and `DEF-276`; `DEF-268` is the remaining active structural blocker before the manual release gate can be completed.
- `DEF-268` is `In Review + Needs Manual Test`: the local operational scripts now share cwd/config-aware Caddy discovery, cover relative and absolute config argv, and refuse to start a duplicate when shutdown does not complete. Linux `bash -n` and the matcher contract passed via read-only SSH stdin; the fix is not deployed to VPS yet and structurally blocks `DEF-164` until its deployment/manual boundary is verified.
- `DEF-192` is `Done`: the current local working-tree `waffen-tactics-web/backend/events_test_fresh.json` is the explicitly approved source of truth. Its frozen SHA-256, byte size, event count, sequence range, scenario, and event-type coverage are recorded in `events_test_fresh.provenance.json`; no historical VPS or `HEAD` fixture was restored.
- Release acceptance still needs Game View checks at 1280x720 and 1920x1080, backend/frontend revision alignment, deployment/VPS status and logs, and rollback evidence.
- Deployment evidence on 2026-09-10: VPS revision `7fd85e8291673451771f03918d42aae8aa561134` is running while the local source-of-truth HEAD is newer and has not been deployed; backend and Vite listen on `127.0.0.1:8000` and `127.0.0.1:3000`, Caddy owns the public TLS listener, public `/` and `/api/game/traits` return 200, and external direct access to ports 3000/8000 is blocked. The tracked VPS branch is clean, but unrelated untracked VPS drift remains and is not treated as release approval.
- `DEF-226` is `Done`: production backend binding is committed and deployed in `7fd85e8291673451771f03918d42aae8aa561134`; local focused/full backend verification and VPS listener/proxy checks passed.
- `DEF-227` is `Done`: production Vite binding is committed and deployed in `7fd85e8291673451771f03918d42aae8aa561134`; local focused/full frontend verification and VPS listener/proxy checks passed.
- `DEF-228` is `Done`: the public `/health` response retains its stable `status`/`db` shape but returns `db: configured` instead of the absolute database path; a backend regression test proves the path and filename do not cross the HTTP boundary.
- `DEF-229` is `Done`: `tools/scripts/validate_units.py` resolves authoritative datasets from the repository root and validates the canonical nested `modular_effects` trait schema; a regression test proves success from both the repository root and an unrelated working directory. No dataset or archive content was changed.
- `DEF-230` is `Done`: canonical unit-data loading now raises a typed error for missing or malformed skills instead of generating a cost-based production fallback; regression coverage proves missing-skill, parser-exception, and all-52-valid-unit paths.
- `DEF-231` is `Done`: `validate_all_skills.py` now validates canonical skill presence for every unit, including missing, null, and malformed values; regression coverage proves the fail-closed errors and the current 52-unit dataset pass. No dataset or archive content was changed.
- `DEF-232` is `Done`: `validate_units.py` now reports non-object unit and trait records with collection/index context, and duplicate scans tolerate those malformed entries instead of raising. Regression coverage proves malformed-record failure handling; the current 52-unit/21-trait datasets still pass.
- `DEF-233` is `Done`: production combat remains owned by `services.combat_shared`/`services.combat_simulator`; `core.combat_core` and `processors.attack` are explicitly compatibility/test-only, with an AST import guard. Retiring or migrating them later still requires an approved core/damage contract and is not a live gameplay change.
- `DEF-234` is `Done`: `start_combat` now rejects non-object or non-JSON request bodies before token access, preserving the existing HTTP 401 `Missing token` contract; four backend regression assertions cover array, scalar, numeric, and non-JSON inputs. No combat rules, content data, archive, or runtime fixtures were changed.
- `DEF-236` is `Done`: player-action handlers now share a fail-closed JSON-object boundary; invalid non-object, null, malformed, and non-JSON bodies return structured HTTP 400 without invoking action services, while empty bodies remain valid for parameterless actions. The route-boundary suite adds 53 cases and preserves required-field/idempotency behavior.
- `DEF-237` is `Done`: `player_avatar_route` now rejects non-object or malformed non-empty bodies with the existing safe `avatar_fetch_failed` HTTP 400 contract before URL validation, network access, filesystem writes, or DB mapping. Empty-body JWT-derived and valid-object paths remain unchanged; five mocked security regressions cover the invalid shapes.
- `DEF-238` is `Done`: `/api/admin/games` and `/api/admin/teams` now share one parser that rejects malformed or non-positive `page`/`limit` values with stable HTTP 400 before database access, while default and positive pagination response shapes remain unchanged.
- `DEF-239` is `Done`: state, start, reset, and surrender now return the same redacted HTTP 500 contract with request correlation for unexpected lifecycle failures; known reset/surrender not-found responses remain unchanged. No combat rules, content data, archive, or protected fixtures were changed.
- `DEF-240` is `Done`: all player-action routes now map unexpected service/database failures through a shared redacted HTTP 500 contract with request correlation, while validation, business-rule, conflict, and stored-state mappings remain unchanged. No combat rules, content data, archive, or protected fixtures were changed.
- `DEF-241` is `Done`: all admin route families now map unexpected failures through a shared redacted HTTP 500 contract with canonical request correlation, while admin authentication and successful response shapes remain unchanged. No combat rules, content data, archive, or protected fixtures were changed.
- `DEF-242` is `Done`: required synergy, derived-stat, and shop-preview enrichment now raises a typed failure instead of returning partial state; action success responses and cached combat replay map enrichment failures to safe error contracts. Focused regressions and the full backend suite pass. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-243` is `Done`: malformed embedded replay checkpoints now raise a deterministic validation error with event type and sequence context instead of being swallowed; valid checkpoint comparison and canonical replay behavior remain green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-244` is `Done`: persisted player and database-opponent teams are now resolved atomically against canonical unit data; unknown or malformed entries fail closed before synergy/stat preparation instead of being silently skipped. Full backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-245` is `Done`: canonical event callback failures now propagate from every covered emitter through the shared dispatcher, simulator, and backend as typed execution failures; the event sequence remains unchanged on failed delivery, and regression coverage proves no result is produced from a missing canonical event. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-246` is `Done`: runtime expiration now validates and prepares the complete effect-collection removal before reverting state; malformed or unverifiable removal fails closed without emitting `effect_expired`. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-222` is `Done`: canonical death now validates HP and optional simulator-mirror mutation before emitting `unit_died`, with rollback and regression coverage for rejected mutations. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-247` is `Done`: direct and scheduled death processing now fail closed when canonical death mutation fails, preventing a winner or backend result without `unit_died`. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-248` is `Done`: malformed `unit_died`, `unit_heal`/`heal`, `hp_regen`, and `damage_over_time_applied` events now fail closed on missing or unknown unit identity; unsupported replay event types also fail deterministically while explicit metadata/legacy branches remain intentional no-ops. Focused and full backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-249` is `Done`: the frontend reducer now rejects missing or unknown identities for death/heal/regen/DoT application and the replay boundary records a typed desync, stops its timer, and leaves the last valid playhead/state intact. Focused and full frontend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-250` is `Done`: the remaining frontend mutating handlers now validate supplied attack targets/attackers and required unit identities before mutation, including atomic multi-identity `unit_attack` validation. Full frontend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-251` is `Done`: unsupported frontend replay event types now fail with a typed sequence-aware validation error, while explicit metadata/no-op event branches remain intentional and tested. Full frontend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-252` is `Done`: the canonical damage emitter now verifies target HP/shield and supplied HP-mirror mutations, rolls back partial state, and fails before event delivery on mutation failure. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-253` is `Done`: canonical mana emitters now verify recipient and supplied mirror mutations, roll back partial state, and fail before event delivery on mutation failure. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-254` is `Done`: the canonical stun emitter now verifies target stun fields and effect installation, rolls back partial state, rejects dead targets before mutation, and fails before event delivery on mutation failure. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-255` is `Done`: HP-regen rewards now mutate each intended recipient exactly once through the canonical emitter path when events are collected, with no-callback parity preserved and team event identities aligned to recipients. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-256` is `Done`: `emit_regen_gain` now verifies the canonical HP-regen mutation, rolls back on failure, and skips dead targets before event delivery. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-257` is `Done`: `emit_shield_applied` now verifies both shield and effect writes, rolls back partial state on rejected or silently ignored effect mutation, and emits no success event after failure. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-258` is `Done`: persistent `emit_stat_buff` mutations now verify the immediate stat and effect writes, roll back partial state on rejected or silently ignored effect mutation, and emit no success event after failure. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-259` is `Done`: `emit_effect_applied` now commits and verifies one final replacement collection before emitting lifecycle events, rolls back rejected or silently ignored writes, and preserves expiration-before-application ordering on success. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-260` is `Done`: DoT expiration now validates that the intended effect was actually removed before emitting `damage_over_time_expired`; removal failures and malformed returned effects fail closed without a false expiration event. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-261` is `Done`: per-second HP regeneration now mutates canonical unit HP before committing the corresponding simulator mirror, rolls back canonical HP on downstream event failure, and preserves direct no-callback mirror compatibility. Symmetric focused tests cover valid Team A/Team B commits, rejected setters, callback rollback, and no-callback behavior. Full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-262` is `Done`: Team A and Team B now use one shared per-second buff processor path, with explicit side/index boundaries and a dedicated canonical-first HP/mirror helper. Existing formulas, event payloads, dead-unit handling, mana routing, and amplifier behavior are preserved. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-263` is `Done`: start-of-combat per-round HP buffs now use one shared Team A/Team B helper, mutate canonical HP before the simulator mirror, commit logs only after success, roll back after callback failure, and flow through the simulator event sink. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-265` is `Done`: live modular object-unit stat-buff rewards no longer pre-mutate attack/defense before canonical emission, singular `stat` rewards are accepted alongside `stats`, percentage rewards retain the percentage contract, and successful permanent deltas feed `permanent_buffs_applied`. Focused and full shared-core/backend verification is green. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-266` is `Done`: canonical modular death effects now route through `SynergyEngine.get_active_effects()` and the shared modular processor, with static pre-application and wrong-side triggering covered. Automated verification is green; release Game View/deployment evidence remains separately tracked. No content datasets, balance values, legacy archive, or protected fixtures were changed.
- `DEF-267` is `Done`: canonical periodic and threshold records now use one shared simulator lifecycle with mirror-aware canonical emissions and deterministic trigger state. Automated verification is green; release Game View/deployment evidence remains separately tracked. No periodic/threshold content values were changed.
- `DEF-270` is `Done`: the false-green frontend replay placeholder and its stale absolute TODO path were removed; the canonical repository-relative replay suite remains the owner of real event-dump coverage. Frontend Vitest (127 tests at completion), typecheck, and lint pass. No fixtures, schemas, combat rules, or deployment state were changed.
- `DEF-271` is `Done`: `deploy.ps1 -RunTests` now runs the strict core/backend suites plus frontend typecheck, lint, Vitest, and production build before any remote sync; a script contract test prevents regression to the narrower gate. No VPS mutation or runtime/content change was made.
- `DEF-272` is `Done`: `unit_stunned` now fails closed at the backend SSE mapper and frontend replay reducer when the canonical `effect_id` is missing, blank, or non-string; valid IDs remain removable by `effect_expired`. Full strict core/backend/frontend verification passes. No emitter, content, or deployment behavior was changed.
- `DEF-273` is `Done`: id-less runtime effects now fail before DoT tick/expiration or regular expiration mutation, canonical expiration emitters reject invalid IDs, and the SSE mapper rejects malformed expiration identities. Focused and full strict core/backend verification is green; frontend typecheck, lint, Vitest, and build are also green. No content, approved local replay fixture, legacy archive, or deployment state was changed.
- `DEF-274` is `Done`: backend replay now applies one non-empty-string `effect_id` contract across snapshots and effect lifecycle/application handlers, rejecting malformed identities before state mutation. Focused reconstructor and full backend verification are green. No content, approved local replay fixture, legacy archive, frontend, or deployment state was changed.
- `DEF-275` is `Done`: backend SSE mapping now applies the same non-empty-string `effect_id` contract to all stateful effect payloads, including `effect_applied`, `shield_applied`, `damage_over_time_applied`, `stat_buff`, `damage_over_time_tick`, and embedded `effect_applied` identities. Focused mapper and full backend verification are green. No content, approved local replay fixture, legacy archive, frontend, or deployment state was changed.
- `DEF-276` is `Done`: frontend replay now uses one typed non-empty-string `effect_id` contract across stateful effect application, ticking, stun, and expiration branches; invalid outer/embedded identities fail before mutation and stop the replay boundary instead of advancing the playhead. Frontend typecheck, lint, 162 Vitest tests, and production build pass. No content, approved local replay fixture, legacy archive, or deployment state was changed.
- `DEF-277` is `Done`: the frontend real replay suite no longer relies on a vacuous fresh-dump effect-ID assertion; it now replays deterministic application, tick, and expiration events for stat buffs, shields, generic effects, stuns, and DoT identities, while real-dump initialization failures are hard errors. Focused replay tests, frontend typecheck, lint, 162 Vitest tests, and production build pass. The approved DEF-192 fixture remains unchanged.
- `DEF-235` is `Backlog + Needs User`: `skip_per_round_buffs` currently gates per-second processing while the start-of-combat per-round block remains unconditional. No runtime change was made; the intended round/trait timing contract must be chosen before splitting or renaming the controls.
- `DEF-199` is `Backlog + Needs User`: the author must choose fixed, capped, or intentionally round-scaled milestone rewards before economy sign-off. No numeric balance change has been made for that decision.
- `DEF-269` is `Backlog + Needs User`: the shared stat-buff runtime has duplicated recipient/application paths and silently maps unsupported targets to `self`; canonicalization awaits the explicit unknown-target contract decision recorded in the issue.
- New-set authoring and contract work remains author-led (`Needs User`/`Manual-only`); do not promote draft content into active datasets without approval.

## Next continuation

1. Finish `DEF-268`, then deploy and verify the operational restart behavior when deployment is explicitly authorized.
2. Complete the manual/runtime portion of `DEF-164` (Game View at 1280x720 and 1920x1080 plus release-owner sign-off), without treating green automated tests or HTTP checks as visual runtime proof.
3. Resolve the author decisions in `DEF-199` and `DEF-235` when available; keep new-set authoring gated behind approval.
4. After the gameplay/economy contracts are approved, rerun the seeded audits and compare their JSON artifacts before any permanent tuning.

## Legacy boundary

`archive/sets/legacy-2026-09-04/` remains historical and compatibility reference material. It is not an active dataset and has not been modified by the current runtime work.
